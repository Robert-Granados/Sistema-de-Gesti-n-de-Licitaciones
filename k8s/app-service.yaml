apiVersion: v1
kind: Service
metadata:
  name: licitaciones-app
  namespace: licitaciones
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: licitaciones-app
  ports:
    - name: http
      port: 80
      targetPort: http
